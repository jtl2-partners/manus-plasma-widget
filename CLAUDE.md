## CI/Workflow Rules
- Never modify any files in .github/workflows/ unless the user explicitly says "update workflows" or "modify workflows"
- Never change runs-on values
- Never add, remove or modify concurrency blocks
- Never add, remove or modify workflow triggers (on:)
- Never create new workflow files unless explicitly asked
